<Window x:Class="WPF_Practice.Advanced_Controls.DataGrid"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF_Practice.Advanced_Controls"
        mc:Ignorable="d"
        Title="DataGrid" Height="300" Width="300">
    <Grid>
        <DataGrid Name="dgEmployees" AlternatingRowBackground="LightBlue"
                  AlternationCount="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Name" Binding="{Binding Name}"/>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>
